<IfModule mod_rewrite.c>
  RewriteEngine On RewriteBase / # Si el archivo o directorio no existe,
  redirige a index.html RewriteCond %{REQUEST_FILENAME} !-f RewriteCond
  %{REQUEST_FILENAME} !-d # Redirige todo a index.html RewriteRule ^.*$
  /index.html [L,QSA]
</IfModule>
